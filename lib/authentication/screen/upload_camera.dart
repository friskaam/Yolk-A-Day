import 'package:flutter/material.dart';

class UploadCamera extends StatelessWidget {
  const UploadCamera({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Camera');
  }
}
